using System.Numerics;

namespace MyEngine.Components;

/// <summary>
/// Глобальный ambient-свет. Одна сущность с этим компонентом
/// задаёт базовый уровень освещения всего мира.
/// Если сущности нет — используется белый (нет затемнения).
///
/// Обычная ночь: (0.15, 0.15, 0.25) — тёмно-синий.
/// Пещера: (0.1, 0.1, 0.15) — почти чёрный.
/// День: (1, 1, 1) — полное освещение.
/// </summary>
public sealed class AmbientLight
{
    public Vector3 Color = new(1f, 1f, 1f);
}